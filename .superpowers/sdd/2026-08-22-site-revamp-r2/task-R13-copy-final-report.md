# Task R13 — owner-final zh copy (42 定稿 entries)

Branch `codex/site-revamp-r2-20260822`, base 79878af. Source of truth:
`output/r3-align/copy-intent-deck.md` (every entry's 定稿 line applied verbatim).

## Status

**37 / 42 applied ✓ · 0 rewording deviations · 5 entries blocked (A-1..A-5, generator absent — see below).**

Every applied string is byte-identical to its 定稿 (applied via an exact-match
replacement script that asserted each current string occurred exactly once in
its 位置 file before substituting; script: scratchpad `apply-copy-final.py`,
run output `APPLIED 37 edits across 13 files`).

## Per-entry ledger

| 条目 | 文件 | 状态 |
|---|---|---|
| 0-1 | `src/lib/i18n.ts` `homeSecondary` | 应用✓ |
| 0-2 | `src/lib/i18n.ts` `homeMethodology` | 应用✓ |
| 0-3 | `src/lib/site-config.ts` `description.zh` | 应用✓（按勘误：替换的是 freshness-fix 过渡串「大模型 Agent 与 AI 应用系统、后端与数据工程——…」，非清样「现行中文」栏） |
| 0-4 | `src/components/CommandPalette.tsx` 空结果提示 | 应用✓ |
| 0-5 | `src/components/CommandPalette.tsx` Ask 按钮 | 应用✓ |
| 1-1 … 1-5 | `src/lib/home-stats.ts` `STAT_TEXT_ZH` 五条 | 应用✓（五条全部） |
| 3-1 | `src/components/forge/BoundaryMatrix.tsx` 模板串 | 应用✓（保留代码既有 template-literal 机制：`${yesCount}`/`${noCount}` 原位插值，仅周围文字改为定稿；tsc 编译通过） |
| 4-1 | `src/lib/projects.ts` release-guardian `problem.zh` | 应用✓ |
| 4-2 | `src/lib/projects.ts` `architecture[0].detail.zh` | 应用✓ |
| 4-3 | `src/lib/projects.ts` `provenance[1].zh` | 应用✓ |
| 4-4 | `src/lib/projects.ts` `provenance[2].zh` | 应用✓ |
| 4-5 | `src/components/guardian/GuardianPage.tsx` 拦截脚注 | 应用✓（保留代码既有 JSX 插值机制：两个 `{fmtMs(…)}` 原位；`disposition` 槽位保留现有 zh 本地化三元 `{blockOutcome.disposition === "rejected" ? "已拒绝" : blockOutcome.disposition}` —— 定稿写的是裸 `{blockOutcome.disposition}`，按任务规则「preserve the code's mechanism, only the surrounding text changes」保留三元；周围文字与定稿逐字一致） |
| 6-1 … 6-4 | `src/lib/projects.ts` rag-quality-lab 四条 | 应用✓ |
| 6-5 | `src/components/rag/RagManifestDriftLab.tsx` | 应用✓ |
| 7-1, 7-2 | `src/lib/projects.ts` privacy-preflight | 应用✓ |
| 8-1 | `src/components/ask/AskPage.tsx` `failedMessage` | 应用✓ |
| 10-1 | `src/components/crossover/SqlWorkbench.tsx` | 应用✓ |
| 10-2 | `src/components/crossover/CrossoverExhibit.tsx` aria-label | 应用✓ |
| 11-1 … 11-3 | `src/lib/projects.ts` margin-control-tower | 应用✓（11-1 原 zh 槽为英文原句，locale 纯度缺陷已消除） |
| 11-4, 11-5 | `src/components/analytics/MarginControlTower.tsx` | 应用✓ |
| 12-1 … 12-3 | `src/lib/projects.ts` credit-policy-desk | 应用✓（12-1 同为 locale 纯度缺陷，已消除；12-3 与 11-3 结构平行、用词一致，已核对） |
| 12-4 … 12-6 | `src/components/analytics/CreditPolicyLab.tsx` | 应用✓ |
| A-1 … A-5 | `src/lib/structural-copy.ts`（生成文件） | **阻塞，未应用**（见下） |

A-1..A-4 定稿前半句「受发布边界限制：私有路径与真实符号不展示。影响：」四条逐字相同——已核对（阻塞前即验证，供解除阻塞后实施用）。

## A-1..A-5 blocker（按规则 STOP，未改写、未手改）

清样与任务规则均要求：改生成器源数据包后重新生成，**不得手改** `src/lib/structural-copy.ts`。
但生成器在本机不存在：

- 文件头注明 `Generated by goal2-generate-structural-copy.mjs`；该脚本不在
  `package.json` scripts、不在 `scripts/`、全仓 `find`/`grep` 无、`git log --all`
  （含 `-S` 内容检索）从未提交过——`structural-copy.ts` 只以快照形式随
  821ac7b / 20d1685 两次提交进入本仓。
- 其源数据包（`goal2-structural-prose-final392.json`、
  `goal2-structural-prose-final392-bindings.json`、
  `goal2-native447-final-candidates-v5.json`，sha256 见
  `src/lib/structural-copy.generation-report.json`）在本机全盘检索
  （home 目录、姊妹工作副本 portfolio-site-lanes / portfolio-site-motion-spike /
  release-guardian-portfolio 等、Spotlight）均无。

**解除路径**：从原生成环境取回生成器与三个数据包（sha256 可对
generation-report 校验），改包中五条 zh 后重跑生成器（会同步更新
`structural-copy.ts` / `structural-copy.test-fixture.json` /
generation-report 的 packetSha256）。五条定稿文本已在清样中，无需再拟。

## Test / gate retargets（全部如实改向定稿文本）

1. `tests/e2e/quality.spec.ts` L399 — zh metadata description 断言由旧串改为
   0-3 定稿「主打 LLM Agent 与 AI 应用系统，后端和数据工程作支撑。项目可以上手、
   回放实录，也说清能证明到哪一步。」（唯一一处需改向的测试；已对 tests/ 与
   scripts/ 全量 grep 旧串及其特征片段，无其他命中）。
2. `scripts/verify-performance-budget.mjs` — R13 re-pin 第八次：共享 chunk 因
   0-3/0-4/0-5 文案长度变化增长（"/" +74、/artifact +88 gzip 字节），两个零余量
   provisional initial pin 触顶，按文档流程在三次连续复现（183,597 / 230,344
   逐字节一致）后重钉："/" 183,523 → 183,597；/artifact 230,256 → 230,344。
   route-own 全部未动；裁定注释已随钉写入脚本。

非改向、仅记录：`src/data/assistant-knowledge.generated.json`、
`src/data/generated/ask-recorded-answers.json` 等生成物中仍含旧串——属并行
assistant-knowledge/question-bank 任务车道（本任务明确不触碰），其下次
re-pin/再生成会拾取新串。`docs/superpowers/specs/2026-08-21-site-revamp-design.md`
中的旧英文摘要为历史规格文档，原样保留。

## Gates（全绿）

- `npx tsc --noEmit` ✓ 0 错误
- `npm run lint` ✓ 0 错误（12 条既有 warning，均在本任务范围外文件）
- `npm run build` ✓
- `npm run check:localization` ✓ 全链（gate-wiring 测试、COPY_LINT_PASS
  files=150、numeric parity 对 :4173 实跑、glyph subset PASS——新引入的
  「」等字形全部在已发布子集内，无需扩集）
- `node scripts/verify-performance-budget.mjs` ✓ 12/12（re-pin 后）
- `npx playwright test tests/e2e/quality.spec.ts` ✓ 18 passed / 0 failed（30
  skipped 为非当前 project 的常规跳过）

## zh-wrap 390w 目测

`output/r3-align/copy-final-shots/`（`scripts/r3-copy-final-shots.mjs` 采集，
zh locale，390×844）：home 统计条 1-2 / 1-4 / 1-5 三条长定稿换行自然、无溢出、
数字完整。**11-3 / 12-3 无法在页面目测**：margin-control-tower 与
credit-policy-desk 已是独立重建路由（Task L2/L4），不再渲染旧
ProjectProof 的 provenance 列表——两条定稿只存在于数据层（projects.ts 序列化
prop 与 assistant 知识），全站无可见渲染面（已逐路由验证：构建 HTML 去
script 后无、hydration 后 innerText 亦无）。文本本身已逐字应用并通过全部
文案门禁；若后续恢复 provenance 渲染面，无需再改文案。

## Deviations

**文案改写偏差：0。** 全部 37 条应用串与定稿逐字一致；4-5 的 disposition
插值三元与 11-3/12-3 无渲染面两点为机制/验证层面的说明（见上），不涉及对
任何定稿文字的改动；A-1..A-5 按「不得手改生成文件」规则整体阻塞，等待
生成器与数据包。
